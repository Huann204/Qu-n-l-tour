extends ../layouts/default.pug
include ../mixins/product-item.pug

block styles
  link(rel="stylesheet", href="/assets/css/news.css")

block main
  main.news
  // Hero
  section.hero
    .container(class="hero__inner")
      div(class="hero__content")
        span.badge
          i.fa-solid.fa-plane-departure
          | Bản tin du lịch
        h1 Khám phá thế giới — cập nhật từng ngày
        p
          | Tin nóng, bí kíp săn sale, review điểm đến, lịch lễ hội, và tất
          |   tần tật để bạn “chốt tour” không lăn tăn.
        form.searchbar(
          onsubmit="return false;" 
          action="#"
          )
          i.fa-solid.fa-magnifying-glass
          input(
            aria-label="Tìm kiếm tin tức" 
            placeholder="Tìm bài viết, điểm đến, ưu đãi..." 
            type="text"
            )
          button.btn.btn--primary(type="button") Tìm
        ul(class="hero__highlights")
          li
            i.fa-regular.fa-clock
            | Cập nhật hằng ngày
          li
            i.fa-solid.fa-fire
            | Hot deal bay/vé
          li
            i.fa-solid.fa-earth-asia
            | Toàn cầu
      div(class="hero__visual")
        img(
          alt="Ảnh minh họa du lịch" 
          src="https://images.unsplash.com/photo-1526778548025-fa2f459cd5c1?q=80&w=1600&auto=format&fit=crop"
          )
    div(
      aria-hidden="true" 
      class="hero__wave"
      )
  // Content
  section.container.layout
    .content
      // Featured
      article.featured
        a(
          href="#" 
          class="featured__image"
          )
          img(
            alt="Bali mùa thu - săn hoàng hôn" 
            src="https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?q=80&w=1600&auto=format&fit=crop"
            )
          span.chip Bí kíp săn vé
        div(class="featured__body")
          a.category(href="#")
            i.fa-solid.fa-tag
            | Kinh nghiệm
          h2(class="featured__title")
            a(href="#") Bali mùa thu: săn hoàng hôn tím & combo resort giá mềm
          p(class="featured__desc")
            | Hướng dẫn chọn khu ở, khung giờ check-in “cháy máy”, lịch trình
            |   4N3Đ đủ Chill – Explore – Eat, kèm mẹo đặt vé rẻ.
          .meta
            span
              i.fa-regular.fa-user
              | Huan Travel
            span
              i.fa-regular.fa-clock
              | 14/09/2025
            span
              i.fa-solid.fa-location-dot
              | Bali,
              |   Indonesia
          a.readmore(href="#")
            | Đọc tiếp
            i.fa-solid.fa-arrow-right
      // Grid news
      .grid
        // Card 1
        each item in newBlogList
          article.card
            a(
              href="#" 
              class="card__thumb"
              )
              img(
                alt=item.name
                src=item.avatar
                )
              span.chip.chip--primary Trend
            div(class="card__body")
              a.category(href="#") Tin tức
              h3(class="card__title")
                a(href="#") #{item.name}
              p(class="card__desc")
                | !{item.description}
      // Pagination
      nav.pagination(aria-label="Pagination")
        a.page.disabled(
          aria-disabled="true" 
          href="#"
          )
    // Sidebar
    aside.sidebar
      // Search
      .widget
        h4(class="widget__title")
          i.fa-solid.fa-magnifying-glass
          | Tìm kiếm
        div(class="widget__body")
          .input
            i.fa-solid.fa-magnifying-glass
            input(
              placeholder="Từ khóa..." 
              type="text"
              )
      // Categories
      .widget
        h4(class="widget__title")
          i.fa-solid.fa-folder-open
          | Danh mục
        ul(class="widget__list")
          each item in categoryBlogList
            li
              a(href="/category-blog/:slug")
                i.fa-solid.fa-tag
                | #{item.name}
              span #{item.totalBlog}
      // Trending
      .widget
        h4(class="widget__title")
          i.fa-solid.fa-fire
          | Đang nổi
        ul.trending
          each item in newBlogList
            li
              a(href="/blog/:slug")
                img(
                  alt=item.name
                  src=item.avatar
                  )
                div
                  p #{item.name}
      // Tags
      .widget
        h4(class="widget__title")
          i.fa-solid.fa-hashtag
          | Thẻ
        .tags
          a(href="#") #bali
          a(href="#") #visa
          a(href="#") #europe
          a(href="#") #phuquoc
          a(href="#") #sale
          a(href="#") #momiji
          a(href="#") #budget
          a(href="#") #review
      // Newsletter
      .widget.widget--cta
        h4(class="widget__title")
          i.fa-regular.fa-paper-plane
          | Nhận ưu đãi sớm
        p Đăng ký để nhận tin hot & mã giảm giá mỗi tuần.
        form.newsletter(
          onsubmit="return false;" 
          action="#"
          )
          input(
            aria-label="Email" 
            placeholder="Email của bạn" 
            type="email"
            )
          button.btn.btn--primary(type="button")
            i.fa-regular.fa-paper-plane
            | Đăng ký
        small.muted
          i.fa-regular.fa-circle-check
          | Không spam, hứa
          |   luôn!